#!/usr/bin/env bash
#MISE description="Run the video-streamer webapp"
set -euxo pipefail

# The app starts idle; enter a source URL (and tick "loop") in the web UI.
uv --directory="${MISE_PROJECT_ROOT}/app" run video-streamer \
    --speed-factor=3 \
    --resize-video=600x-1
